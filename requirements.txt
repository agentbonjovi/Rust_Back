# Python backend (python_backend.py) и GUI (gui_app.py) используют ТОЛЬКО
# стандартную библиотеку CPython >= 3.9. Внешние пакеты сознательно не
# добавлены: сравнение скорости с Rust должно отражать производительность
# самого языка, а не C-расширений (numpy / pandas / sklearn).
#
# Минимальная требуемая версия Python: 3.9
#
# Если в будущем понадобятся внешние библиотеки, добавляйте их сюда
# и устанавливайте командой:
#   python3 -m pip install -r requirements.txt
